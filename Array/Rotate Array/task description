Given an integer array nums, rotate the array to the right by k steps, where k is non-negative.

Учитывая целочисленный массив nums, поверните массив вправо на k шагов, где k является неотрицательным.
(нужно совершить циклический сдвиг вправо на k элементов)
