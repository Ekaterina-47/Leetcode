Given an integer n, return a string array answer (1-indexed) where:
    answer[i] == "FizzBuzz" if i is divisible by 3 and 5.
    answer[i] == "Fizz" if i is divisible by 3.
    answer[i] == "Buzz" if i is divisible by 5.
    answer[i] == i (as a string) if none of the above conditions are true.

Учитывая целое число n, вернуть массив строк answer( 1-индексированный ), где :
    answer[i] == "FizzBuzz", если i делится на 3и 5.
    answer[i] == "Fizz", если i делится на 3.
    answer[i] == "Buzz",если i делится на 5.
    answer[i] == i (как строку), если ни одно из вышеуказанных условий не верно.

